// Not using lit any more

// import { unsafeHTML } from "../../../node_modules/lit-html/lib/unsafe-html";

// export function style(path: string)
// {
// 	return fetch(path).then(x => x.text()).then(x => unsafeHTML(`<style>${x}</style>`));
// }